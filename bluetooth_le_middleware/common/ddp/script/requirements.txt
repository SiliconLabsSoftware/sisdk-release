#
# These are the python packages that needs to be installed in order to run
# the scripts in this directory.
#
# Install can be done using pip:
# python -m pip install -r requirements.txt
#
pylink-square>=1.1.0
